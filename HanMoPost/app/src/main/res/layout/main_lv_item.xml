<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/postThumbnailImg"
        android:layout_width="52dp"
        android:layout_height="52dp"
        app:srcCompat="@mipmap/ic_launcher_round"

        android:layout_marginBottom="4dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/postTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="제목"
        android:textAlignment="viewStart"
        android:textSize="16sp"


        app:layout_constraintBottom_toBottomOf="@+id/postThumbnailImg"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.05"
        app:layout_constraintStart_toEndOf="@+id/postThumbnailImg"
        app:layout_constraintTop_toTopOf="@+id/postThumbnailImg"
        app:layout_constraintVertical_bias="0.1" />

    <TextView
        android:id="@+id/postUserName"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        app:layout_constraintBottom_toBottomOf="parent"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.063"
        app:layout_constraintStart_toEndOf="@+id/postThumbnailImg"
        app:layout_constraintTop_toBottomOf="@+id/postTitle"
        tools:text="닉네임" />

    <TextView
        android:id="@+id/postTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="18:00"
        android:textSize="11sp"

        app:layout_constraintBottom_toBottomOf="@+id/postUserName"
        app:layout_constraintStart_toEndOf="@+id/postUserName"
        app:layout_constraintTop_toTopOf="@+id/postUserName" />

</androidx.constraintlayout.widget.ConstraintLayout>